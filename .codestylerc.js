module.exports = {
    // vue / aliss / wxss / library / node / taro
    projectType: 'vue',
    // 使用了typescript的项目，添加如下配置
    // typescript: {
    //   tsconfigRootDir: __dirname,
    //   project: ['./tsconfig.json'],
    // }
  };